import { countLonelyNumbers } from "./lonely-numbers-in-matrix";

const testArr = [
    [0, 0, 0, 0],
    [0, 1, 0, 5],
    [0, 0, 0, 0],
];

const result = countLonelyNumbers(testArr);

console.log(result);